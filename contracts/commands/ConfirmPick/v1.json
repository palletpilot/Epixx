{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "lagerkraft:commands/ConfirmPick/v1",
  "type": "object",
  "additionalProperties": true,
  "required": ["task_id", "tote_id", "tote_lpn", "qty"],
  "properties": {
    "task_id": { "type": "string", "format": "uuid" },
    "tote_id": { "type": "string", "format": "uuid" },
    "tote_lpn": { "type": "string", "minLength": 1 },
    "qty": { "type": "string", "minLength": 1 }
  }
}
